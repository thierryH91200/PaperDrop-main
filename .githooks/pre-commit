#!/bin/zsh
# Pre-commit: lint + tests (enable with: git config core.hooksPath .githooks)
# Delegates to the Makefile so the commands have one home.
set -e
cd "$(git rev-parse --show-toplevel)"
make lint
make test
